# Votee

Votee - 投票票

#### Usage

- `git clone https://github.com/QAQrz/Votee.git`
- Rename `src/config_default.ts` to `src/config.ts` and edit it
- `npm install`
- `npm run build`
- `npm run start`
